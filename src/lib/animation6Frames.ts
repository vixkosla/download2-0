export const animation6Frames: string[] = Array.from({ length: 6855 - 6762 + 1 }, (_, i) => {
  // Frames are named IMG_6762.PNG up to IMG_6855.PNG located in public/animation6/
  const frameNumber = 6762 + i;
  return `/animation6/IMG_${frameNumber}.PNG`;
});


